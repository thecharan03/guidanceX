<!DOCTYPE html>
<html lang="en">
<head>
  <title><%= course.name %> | GuidanceX</title>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <!-- Bootstrap 5 CSS CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <!-- Bootstrap Icons CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.9.1/font/bootstrap-icons.css" rel="stylesheet" />
  <style>
    body {
      background: linear-gradient(135deg, #121215, #1c1d2a);
      color: white;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      min-height: 100vh;
      margin: 0;
      padding-top: 72px;
    }
    nav.navbar-custom {
      background: rgba(20, 20, 30, 0.9);
      backdrop-filter: saturate(180%) blur(10px);
      position: fixed;
      width: 100%;
      top: 0;
      border-bottom: 1px solid #333847;
      z-index: 1050;
      padding: 0 46px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      height: 70px;
    }
    .navbar-brand {
      display: flex;
      align-items: center;
      gap: 10px;
      font-weight: 800;
      font-size: 1.55rem;
      color: #ff6500;
      letter-spacing: 0.07em;
      user-select:none;
      text-decoration: none;
    }
    .navbar-brand svg {
      width: 42px;
      height: 42px;
      fill: none;
      stroke: #ff6500;
      stroke-width: 2.75;
      filter: drop-shadow(0 0 6px #ff650044);
    }
    .nav-links {
      display: flex;
      gap: 28px;
      font-weight: 600;
      font-size: 1em;
    }
    .nav-links a {
      color: #ddd;
      text-decoration: none;
      border-radius: 28px;
      padding: 8px 22px;
      transition: background 0.3s, color 0.3s;
    }
    .nav-links a:hover {
      background: #ff650023;
      color: #ff6500;
    }
    .nav-actions {
      display: flex;
      gap: 18px;
      align-items: center;
    }
    .nav-actions a button {
      border-radius: 16px;
      font-weight: 700;
      font-size: 1.12rem;
      padding: 11px 33px;
      border: none;
      background: linear-gradient(90deg, #ff6500 55%, #ffa51d 100%);
      color: #fefefe;
      cursor: pointer;
      box-shadow: 0 7px 24px #ff8a249e;
      transition: box-shadow 0.3s ease, background 0.3s ease;
      user-select:none;
    }
    .nav-actions a button:hover {
      background: linear-gradient(90deg, #fea014 60%, #ff6502 95%);
      box-shadow: 0 10px 38px #ff9c2992;
    }
    .container {
      max-width: 900px;
      margin: 24px auto 54px auto;
      padding: 0 20px;
    }
    h1.course-title {
      font-weight: 700;
      font-size: 2.8rem;
      margin-bottom: 22px;
      color: #ff6500;
      letter-spacing: 0.06em;
      user-select:none;
      text-shadow: 0 0 24px #f28a19aa;
      text-align: center;
    }
    .course-expert {
      display: flex;
      align-items: center;
      gap: 20px;
      margin-bottom: 32px;
      user-select:none;
    }
    .expert-photo {
      width: 92px;
      height: 92px;
      border-radius: 50%;
      object-fit: cover;
      border: 3px solid #ff6500;
      box-shadow: 0 0 16px #ff75007a;
    }
    .expert-name {
      font-weight: 700;
      font-size: 1.35rem;
      color: #ffbb51;
    }
    .course-details {
      font-size: 1.17rem;
      margin-bottom: 28px;
      color: #d9d9d9cc;
      line-height: 1.5;
    }
    .course-info {
      font-weight: 600;
      font-size: 1.05rem;
      color: #ffc47d;
      margin-bottom: 8px;
    }
    .btn-buy {
      background: linear-gradient(90deg, #ff6500 55%, #ffa51d 100%);
      border: none;
      border-radius: 18px;
      padding: 16px 44px;
      font-weight: 700;
      font-size: 1.3rem;
      color: #fff;
      cursor: pointer;
      box-shadow: 0 6px 36px #ff7f1baa;
      transition: background 0.25s ease;
      user-select:none;
      display: block;
      margin: 0 auto;
    }
    .btn-buy:hover {
      background: linear-gradient(90deg, #ffa51d 60%, #ff6500 100%);
      box-shadow: 0 8px 44px #ffb256ee;
    }
    .course-desc {
      font-size: 1.11rem;
      line-height: 1.58;
      color: #e4e3e3cc;
      margin-bottom: 44px;
      text-align: justify;
      user-select:none;
    }
    @media (max-width: 575px) {
      h1.course-title {
        font-size: 2rem;
      }
      .course-expert {
        flex-direction: column;
        text-align: center;
      }
      .expert-name {
        font-size: 1.25rem;
      }
      .expert-photo {
        width: 82px;
        height: 82px;
        margin-bottom: 12px;
      }
    }







    .gx-mini-footer{
  width:100%;
  background:#0c0c10;
  border-top:1px solid #232326;
  padding:10px 40px;
  font-family:'Montserrat','Segoe UI',sans-serif;
  font-size:0.8rem;
  color:#a1a1aa;
}

.gx-mini-inner{
  max-width:1180px;
  margin:0 auto;
  display:flex;
  justify-content:space-between;
  align-items:center;
  gap:10px;
}

.gx-mini-links a{
  color:#c7c7d1;
  text-decoration:none;
  margin-left:16px;
  transition:color .18s;
}

.gx-mini-links a:hover{
  color:#ff7700;
}

@media (max-width:700px){
  .gx-mini-inner{
    flex-direction:column;
    align-items:flex-start;
  }
  .gx-mini-links a{
    margin-left:0;
    margin-right:16px;
  }
}

  </style>
</head>
<body>
  <nav class="navbar-custom d-flex justify-content-between px-4">
    <a href="/" class="navbar-brand" aria-label="GuidanceX Home">
      <svg aria-hidden="true" width="42" height="42" viewBox="0 0 55 39" fill="none" xmlns="http://www.w3.org/2000/svg">
        <rect x="12" y="8" width="28" height="8" rx="3.2" fill="#ff6500" stroke="#fff" stroke-width="2"/>
        <rect x="16.5" y="16" width="19" height="9" rx="3.2" fill="#232326" stroke="#ff6500" stroke-width="1.5"/>
        <polygon points="20,25 30,32 26,34 36,37 28,27" fill="#fff" stroke="#ff6500" stroke-width="1"/>
        <circle cx="28" cy="12" r="3.2" fill="#fff" stroke="#ff6500" stroke-width="2"/>
      </svg>
      GuidanceX
    </a>
    <div>
      <a href="/topics" class="nav-links mx-3">Courses</a>
      <a href="/about" class="nav-links mx-3">About</a>
      <a href="/help" class="nav-links mx-3">Help</a>
      <a href="/login" class="nav-links mx-3">Log In</a>
      <a href="/help" class="btn btn-primary">Help</a>


    </div>
  </nav>

  <main class="container">
    <h1 class="course-title"><%= course.name %></h1>
    <div class="course-expert">
      <img src="<%= course.expertPhoto %>" alt="Expert <%= course.expertName %>" class="expert-photo" />
      <div>
        <div class="expert-name"><%= course.expertName %></div>
        <div class="course-info">Duration: <%= course.duration %> days</div>
        <div class="price-tag">Price: ₹<%= course.price %></div>
      </div>
    </div>
    <div class="course-desc"><%= course.description %></div>
    <button class="btn-buy" type="button" onclick="window.location='/buy/<%= course.topicName %>/<%= course.name %>'">Buy Now</button>
  </main>






  <footer class="gx-mini-footer">
  <div class="gx-mini-inner">
    <span>© <span id="gx-year"></span> GuidanceX. All rights reserved.</span>
    <span class="gx-mini-links">
      <a href="#">Privacy Policy</a>
      <a href="#">Terms of Service</a>
      <a href="#">Cookie Policy</a>
    </span>
  </div>
</footer>

<script>
  document.getElementById('gx-year').textContent = new Date().getFullYear();
</script>

</body>
</html>
