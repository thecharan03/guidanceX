<!DOCTYPE html>
<html lang="en">
<head>
  <title><%= topic %> Subtopics | GuidanceX</title>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <!-- Bootstrap 5 CSS CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <!-- Bootstrap Icons CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.9.1/font/bootstrap-icons.css" rel="stylesheet" />
  <style>
    body {
      background: linear-gradient(135deg, #121215, #1c1d2a);
      color: white;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      min-height: 100vh;
      margin: 0;
      padding-top: 72px; /* For fixed navbar */
    }
    .navbar-custom {
      background: rgba(20, 20, 30, 0.9);
      backdrop-filter: saturate(180%) blur(10px);
      position: fixed;
      width: 100%;
      top: 0;
      border-bottom: 1px solid #333847;
      z-index: 1050;
    }
    .navbar-brand, .nav-link {
      color: #57a8ff;
      cursor: pointer;
      transition: color 0.3s ease;
    }
    .nav-link:hover, .navbar-brand:hover {
      color: #aacaff;
      text-decoration: none;
    }
    .container {
      max-width: 1100px;
    }
    h1.page-title {
      font-weight: 700;
      margin-bottom: 24px;
      letter-spacing: 0.05em;
      text-shadow: 0 0 18px #3b6effaa;
    }
    .card-subtopic {
      background: #232a40cc;
      border-radius: 18px;
      padding: 22px;
      box-shadow: 0 4px 28px #0059ff22;
      transition: background 0.3s ease, transform 0.25s ease;
      cursor: pointer;
      height: 100%;
      display: flex;
      flex-direction: column;
    }
    .card-subtopic:hover {
      background: #1a2040dd;
      transform: scale(1.06);
      box-shadow: 0 6px 42px #0073ffcc;
    }
    .subtopic-header {
      display: flex;
      align-items: center;
      gap: 18px;
      margin-bottom: 14px;
    }
    .subtopic-icon {
      font-size: 2.8rem;
      color: #57a8ff;
      flex-shrink: 0;
    }
    .subtopic-title {
      font-weight: 700;
      font-size: 1.5rem;
      color: white;
      user-select:none;
    }
    .subtopic-desc {
      flex-grow: 1;
      font-size: 1.1rem;
      color: #abcaffcc;
      margin-bottom: 20px;
      user-select:none;
    }
    .price-tag {
      font-weight: 600;
      font-size: 1.25rem;
      color: #57a8ff;
    }
    .btn-buy {
      align-self: start;
      background: #57a8ff;
      border: none;
      border-radius: 16px;
      padding: 10px 28px;
      font-weight: 700;
      color: white;
      cursor: pointer;
      transition: background 0.2s ease;
      box-shadow: 0 4px 18px #004d99bb;
      user-select:none;
    }
    .btn-buy:hover {
      background: #73b0fe;
      box-shadow: 0 6px 28px #0066ccdd;
    }
    @media (max-width: 575px) {
      .subtopic-title {
        font-size: 1.3rem;
      }
      .subtopic-desc {
        font-size: 1rem;
      }
      .price-tag {
        font-size: 1.05rem;
      }
    }
  </style>
</head>
<body>
  <nav class="navbar navbar-custom d-flex justify-content-between px-4">
    <div class="navbar-brand">GuidanceX</div>
    <div>
      <a href="/topics" class="nav-link mx-2">Courses</a>
      <a href="/about" class="nav-link mx-2">About</a>
      <a href="/help" class="nav-link mx-2">Help</a>
      <a href="/login" class="nav-link mx-2">Log In</a>
      


    </div>
  </nav>

  <main class="container mt-5 pt-4">
    <h1 class="page-title">Explore <%= topic %> Subtopics</h1>
    <div class="row g-4">
      <% subtopics.forEach(sub => { %>
        <% 
          // Bootstrap icons based on subtopic name keywords
          let iconClass = 'bi-info-circle';
          if(sub.name.toLowerCase().includes('marketing')) iconClass = 'bi-megaphone';
          else if(sub.name.toLowerCase().includes('digital')) iconClass = 'bi-globe';
          else if(sub.name.toLowerCase().includes('management')) iconClass = 'bi-people';
          else if(sub.name.toLowerCase().includes('analytics')) iconClass = 'bi-bar-chart-line';
          else if(sub.name.toLowerCase().includes('seo')) iconClass = 'bi-search';
        %>
      <div class="col-12 col-md-6 col-lg-4">
        <div class="card-subtopic" tabindex="0" role="button" onclick="window.location='/course/<%= topic %>/<%= sub.name %>'">
          <div class="subtopic-header">
            <i class="subtopic-icon bi <%= iconClass %>" aria-hidden="true"></i>
            <div class="subtopic-title"><%= sub.name %></div>
          </div>
          <p class="subtopic-desc"><%= sub.desc %></p>
          <div class="price-tag">₹<%= sub.price %></div>
          <button class="btn-buy" type="button" onclick="window.location='/buy/<%= topic %>/<%= sub.name %>'; event.stopPropagation();">Buy Course</button>
        </div>
      </div>
      <% }) %>
    </div>
  </main>

  <!-- Bootstrap JS + Popper (optional if using JS components) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
