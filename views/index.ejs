<!DOCTYPE html>
<html lang="en">
<head>
  <title>GuidanceX — Where Mastery Begins</title>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <style>
    /* ---------- GLOBAL ---------- */
    html, body {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      width: 100%;
      height: 100%;
      font-family: 'Montserrat', sans-serif;
      background: #121215;
      color: #f7f7fa;
      scroll-behavior: smooth;
    }

    /* ---------- NAVBAR ---------- */
    .nav-bar {
      width: 100vw;
      padding: 0 60px;
      height: 75px;
      background: rgba(20,20,24,0.93);
      backdrop-filter: blur(2px);
      display: flex;
      align-items: center;
      justify-content: space-between;
      position: fixed;
      top: 0; left: 0;
      z-index: 999;
      border-bottom: 1px solid #232326;
    }

    .nav-logo {
      display: flex;
      align-items: center;
      gap: 15px;
      font-size: 1.45em;
      font-weight: 700;
      color: #fff;
      letter-spacing: 0.04em;
    }

    .nav-links {
      display: flex;
      gap: 34px;
    }

    .nav-link {
      text-decoration: none;
      color: #eee;
      padding: 4px 18px;
      font-size: 1.1em;
      border-radius: 10px;
      transition: 0.2s;
    }
    .nav-link:hover {
      background: #232326;
      color: #ff7700;
    }

    .nav-actions {
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .nav-btn {
      background: linear-gradient(90deg, #ff7700 60%, #f77f3c 100%);
      color: #fff;
      padding: 9px 32px;
      font-size: 1.1em;
      border-radius: 17px;
      border: none;
      cursor: pointer;
      font-weight: 700;
      box-shadow: 0 2px 12px #18081844;
      transition: 0.2s;
    }
    .nav-btn.secondary {
      background:#232326;
      color:#ff7700;
      border:2px solid #ff7700;
    }
    .nav-btn.secondary:hover {background:#ff7700;color:#fff;}

    /* ---------- HAMBURGER ---------- */
    .hamburger {
      display: none;
      flex-direction: column;
      gap: 4px;
      cursor: pointer;
    }
    .hamburger div {
      width: 26px;
      height: 3px;
      background: #fff;
      border-radius: 2px;
      transition: 0.3s;
    }

    /* ---------- FULLSCREEN MENU ---------- */
    .mobile-menu {
      position: fixed;
      top: 0; left: 0;
      width: 100vw;
      height: 100vh;
      backdrop-filter: blur(18px);
      background: rgba(15,15,18,0.65);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: 28px;
      opacity: 0;
      pointer-events: none;
      transition: opacity 0.3s ease;
      z-index: 998;
    }

    .mobile-menu.show {
      opacity: 1;
      pointer-events: all;
    }

    .mobile-link {
      color: #fff;
      font-size: 1.8em;
      text-decoration: none;
      font-weight: 600;
      letter-spacing: 0.03em;
      transition: 0.2s;
    }
    .mobile-link:hover {
      color: #ff7700;
    }

    /* ---------- HERO ---------- */
    .hero {
      min-height: 100vh;
      width: 100vw;
      padding-top: 75px;
      background: radial-gradient(90% 70% at 60% 20%, #232326 12%, #121215 80%);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }

    .hero-core {
      max-width: 1180px;
      width: 96%;
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 70px;
      padding: 64px 0;
    }

    .hero-info {
      flex: 1;
      display: flex;
      flex-direction: column;
      max-width: 530px;
    }

    .hero-title {
      font-size: 3.3em;
      font-weight: 700;
      margin-bottom: 22px;
      line-height: 1.1;
    }

    .hero-desc {
      font-size: 1.3em;
      line-height: 1.45;
      margin-bottom: 36px;
      color: #e3e3ea;
    }

    .hero-action {
      background: linear-gradient(90deg, #ff7700, #f77f3c);
      border: none;
      padding: 18px 44px;
      font-size: 1.15em;
      border-radius: 17px;
      color: #fff;
      font-weight: 700;
      cursor: pointer;
    }

    /* ---------- RESPONSIVE ---------- */
    @media (max-width: 900px) {
      .nav-links, .nav-actions {
        display: none;
      }

      .hamburger {
        display: flex;
      }

      .hero-core {
        flex-direction: column;
        text-align: center;
        gap: 35px;
      }

      .hero-title { font-size: 2.4em; }
      .hero-desc { font-size: 1.15em; }

      .hero-logo svg {
        width: 85vw;
      }
    }
  </style>
</head>

<body>

  <!-- NAVBAR -->
  <nav class="nav-bar">
    <div class="nav-logo">
      <svg viewBox="0 0 55 39">
        <rect x="12" y="8" width="28" height="8" rx="3.2" fill="#ff7700" stroke="#fff" stroke-width="2"/>
        <rect x="16.5" y="16" width="19" height="9" rx="3.2" fill="#232326" stroke="#ff7700" stroke-width="1.5"/>
        <polygon points="20,25 30,32 26,34 36,37 28,27" fill="#fff" stroke="#ff7700" stroke-width="1"/>
        <circle cx="28" cy="12" r="3.2" fill="#fff" stroke="#ff7700" stroke-width="2"/>
      </svg>
      GuidanceX
    </div>

    <div class="nav-links">
      <a class="nav-link" href="/">Home</a>
      <a class="nav-link" href="/login">Courses</a>
      <a class="nav-link" href="#">Pricing</a>
      <a class="nav-link" href="/about">About</a>
    </div>

    <div class="nav-actions">
      <a href="/login"><button class="nav-btn secondary">Log In</button></a>
      <a href="/login"><button class="nav-btn">Get Started</button></a>
    </div>

    <!-- HAMBURGER -->
    <div class="hamburger" onclick="toggleMenu()">
      <div></div><div></div><div></div>
    </div>
  </nav>

  <!-- FULLSCREEN MOBILE MENU -->
  <div class="mobile-menu" id="mobileMenu">
    <a href="/" class="mobile-link">Home</a>
    <a href="/login" class="mobile-link">Courses</a>
    <a href="#" class="mobile-link">Pricing</a>
    <a href="/about" class="mobile-link">About</a>
    <a href="/login" class="mobile-link" style="color:#ff7700;">Login</a>
  </div>

  <!-- HERO -->
  <main class="hero">
    <div class="hero-core">
      <section class="hero-info">
        <div class="hero-title">
          There is a Better Way<br>
          to Learn, Grow and Succeed.
        </div>
        <div class="hero-desc">
          Discover expert mentorship at its finest.  
          GuidanceX is your all-in-one learning hub for business, trading, design, and personal growth — built for those chasing true mastery.
        </div>
        <a href="/login"><button class="hero-action">Explore Courses</button></a>
      </section>

      <section class="hero-logo">
        <svg viewBox="0 0 370 290">
          <!-- your svg unchanged -->
        </svg>
      </section>
    </div>

    <div style="margin-top:20px;opacity:0.7;">
      <span style="color:#ff7700;">GuidanceX</span> – Premium learning crafted for SRM and ambitious creators.
    </div>
  </main>

  <!-- JS -->
  <script>
    function toggleMenu() {
      document.getElementById("mobileMenu").classList.toggle("show");
    }
  </script>

</body>
</html>
