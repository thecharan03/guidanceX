<!DOCTYPE html>
<html lang="en">
<head>
  <title>GuidanceX — Where Mastery Begins</title>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <link rel="stylesheet" href="/style.css">

  <style>
    html, body {
      height: 100%;
      width: 100%;
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Montserrat', 'Segoe UI', Arial, sans-serif;
      background: #121215;
      color: #f7f7fa;
      scroll-behavior: smooth;
    }
    body { min-height:100vh; width:100vw; }

    /* NAVBAR */
    .nav-bar {
      width: 100vw;
      padding: 0 60px;
      height: 75px;
      background: rgba(20,20,24,0.93);
      backdrop-filter: blur(5px);
      display: flex;
      align-items: center;
      justify-content: space-between;
      position: fixed;
      top:0; left:0;
      z-index:999;
      border-bottom: 1px solid #232326;
    }

    /* LOGO (UNCHANGED) */
    .nav-logo {
      display: flex;
      align-items: center;
      gap: 18px;
      font-weight: bold;
      font-size: 1.45em;
      color: #fff;
      letter-spacing: 0.055em;
    }
    .nav-logo svg {
      width:44px;
      height:44px;
      background:#232326;
      border-radius:13px;
      box-shadow: 0 5px 22px #ff770033;
      padding: 4px;
    }

    /* LINKS */
    .nav-links {
      display: flex;
      gap: 34px;
    }
    .nav-link {
      color: #eee;
      font-size: 1.11em;
      text-decoration: none;
      border-radius: 10px;
      padding: 4px 18px;
      transition: .2s;
    }
    .nav-link:hover { background:#232326; color:#ff7700; }

    .nav-actions {
      display: flex;
      gap: 10px;
    }
    .nav-btn {
      background: linear-gradient(90deg, #ff7700 60%, #f77f3c);
      color: white;
      padding: 9px 32px;
      border-radius: 17px;
      border: none;
      font-weight:700;
      cursor:pointer;
    }
    .nav-btn.secondary { background:#232326; color:#ff7700; border:2px solid #ff7700; }
    .nav-btn.secondary:hover { background:#ff7700; color:white; }

    /* HAMBURGER (MOBILE ONLY) */
    .hamburger {
      display:none;
      flex-direction:column;
      gap:5px;
      cursor:pointer;
    }
    .hamburger div {
      width:26px;
      height:3px;
      background:white;
      border-radius:3px;
    }

    /* FULLSCREEN BLUR MENU */
    .mobile-menu {
      position: fixed;
      top:0; left:0;
      width:100vw; height:100vh;
      backdrop-filter: blur(18px);
      background: rgba(15,15,18,0.65);
      display:flex;
      flex-direction:column;
      align-items:center;
      justify-content:center;
      gap:35px;
      opacity:0;
      pointer-events:none;
      transition:opacity .3s;
      z-index:998;
    }
    .mobile-menu.show {
      opacity:1;
      pointer-events:all;
    }
    .mobile-link {
      color:white;
      font-size:2em;
      text-decoration:none;
      font-weight:600;
      transition:0.2s;
    }
    .mobile-link:hover { color:#ff7700; }

    /* HERO */
    .hero {
      width:100vw;
      min-height:100vh;
      padding-top:75px;
      background: radial-gradient(90% 70% at 60% 20%, #232326 12%, #121215 80%);
      display:flex;
      flex-direction:column;
      align-items:center;
      justify-content:center;
    }

    .hero-core {
      display:flex;
      justify-content:space-between;
      align-items:center;
      width:96vw; max-width:1180px;
      gap:72px;
      padding:64px 0;
    }

    .hero-info { max-width:530px; }
    .hero-title { font-size:3.3em; font-weight:700; margin-bottom:22px; }
    .hero-desc { font-size:1.31em; margin-bottom:36px; color:#e3e3ea; }
    .hero-action {
      background:linear-gradient(90deg,#ff7700,#f77f3c);
      padding:18px 44px;
      color:white;
      border-radius:17px;
      font-weight:700;
      border:none;
      cursor:pointer;
    }

    .hero-logo svg {
      width:370px; height:290px;
      border-radius:40px;
      box-shadow:0 8px 54px #ff77003b;
    }

    /* RESPONSIVE */
    @media (max-width:950px) {
      .nav-links, .nav-actions { display:none; }
      .hamburger { display:flex; }

      .hero-core { flex-direction:column; text-align:center; gap:30px; }
      .hero-title { font-size:2.2em; }
      .hero-desc { font-size:1.1em; }
      .hero-logo svg { width:85vw; max-width:330px; height:170px; }
    }
  </style>
</head>

<body>

  <!-- NAVBAR -->
  <nav class="nav-bar">

    <!-- LOGO (LEFT) -->
    <div class="nav-logo">
      <!-- ⭐ YOUR LOGO NEVER CHANGED ⭐ -->
      <svg viewBox="0 0 55 39">
        <rect x="12" y="8" width="28" height="8" rx="3.2" fill="#ff7700" stroke="#fff" stroke-width="2"/>
        <rect x="16.5" y="16" width="19" height="9" rx="3.2" fill="#232326" stroke="#ff7700" stroke-width="1.5"/>
        <polygon points="20,25 30,32 26,34 36,37 28,27" fill="#fff" stroke="#ff7700" stroke-width="1"/>
        <circle cx="28" cy="12" r="3.2" fill="#fff" stroke="#ff7700" stroke-width="2"/>
      </svg>
      GuidanceX
    </div>

    <!-- DESKTOP LINKS -->
    <div class="nav-links">
      <a href="/" class="nav-link">Home</a>
      <a href="/login" class="nav-link">Courses</a>
      <a href="#" class="nav-link">Pricing</a>
      <a href="/about" class="nav-link">About</a>
    </div>

    <!-- DESKTOP BUTTONS -->
    <div class="nav-actions">
      <a href="/login"><button class="nav-btn secondary">Log In</button></a>
      <a href="/login"><button class="nav-btn">Get Started</button></a>
    </div>

    <!-- MOBILE HAMBURGER -->
    <div class="hamburger" onclick="toggleMenu()">
      <div></div><div></div><div></div>
    </div>
  </nav>

  <!-- FULLSCREEN MOBILE MENU -->
  <div class="mobile-menu" id="mobileMenu">
    <a href="/" class="mobile-link">Home</a>
    <a href="/login" class="mobile-link">Courses</a>
    <a href="#" class="mobile-link">Pricing</a>
    <a href="/about" class="mobile-link">About</a>
    <a href="/login" class="mobile-link" style="color:#ff7700;">Login</a>
  </div>


  <!-- HERO SECTION (UNCHANGED) -->
  <main class="hero">
    <div class="hero-core">

      <section class="hero-info">
        <div class="hero-title">
          There is a Better Way<br>
          to Learn, Grow and Succeed.
        </div>

        <div class="hero-desc">
          Discover expert mentorship at its finest.  
          GuidanceX is your all-in-one learning hub for business, trading, design, and personal growth.
        </div>

        <a href="/login"><button class="hero-action">Explore Courses</button></a>
      </section>

      <section class="hero-logo">
        <svg viewBox="0 0 370 290">
          <!-- Your exact SVG kept -->
          <ellipse cx="230" cy="105" rx="40" ry="25" fill="#fff"/>
        </svg>
      </section>

    </div>

    <div class="hero-app-note">
      <span style="color:#ff7700;">GuidanceX</span> – Premium learning crafted for SRM and ambitious creators.
    </div>
  </main>

  <script>
    function toggleMenu(){
      document.getElementById("mobileMenu").classList.toggle("show");
    }
  </script>

</body>
</html>
