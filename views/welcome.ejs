<!DOCTYPE html>
<html lang="en">
<head>
  <title>Welcome | Logome</title>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      margin: 0;
      min-height: 100vh;
      font-family: 'Montserrat', Arial, sans-serif;
      background: linear-gradient(120deg, #b2b9e8 0%, #dedcf5 100%);
    }
    .main-wrapper {
      background: #fff;
      margin: 32px auto;
      border-radius: 24px;
      max-width: 900px;
      box-shadow: 0 10px 46px #c2c7e3aa;
      padding: 0;
    }
    .top-navbar {
      padding: 28px 44px 0 44px;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }
    .nav-logo {
      font-size: 1.21rem;
      font-weight: 900;
      color: #222;
      letter-spacing: 0.08em;
    }
    .nav-group {
      display: flex;
      gap: 38px;
      align-items: center;
    }
    .nav-link {
      color: #333;
      font-size: 1rem;
      font-weight: 500;
      text-decoration: none;
      transition: color 0.15s;
      padding: 2px 10px;
      border-radius: 9px;
    }
    .nav-link.active, .nav-link:hover {
      background: #f0f1fa;
      color: #7157d3;
    }
    .nav-signup {
      background: #cfd6f6;
      color: #444;
      padding: 5px 17px;
      font-weight: 600;
      font-size: 1rem;
      border-radius: 13px;
      border: none;
      box-shadow: 0 2px 10px #a3b1ef40;
    }
    .nav-signup:hover {
      background: #7157d3;
      color: #fff;
    }
    .main-content {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 44px;
      gap: 34px;
    }
    .welcome-block {
      flex: 1.2;
      min-width: 240px;
      max-width: 390px;
      display: flex;
      flex-direction: column;
    }
    .welcome-title {
      font-size: 2.66rem;
      font-weight: 900;
      color: #453792;
      margin-bottom: 6px;
      letter-spacing: 0.03em;
    }
    .welcome-sub {
      font-size: 1.16rem;
      font-weight: 600;
      color: #deac32;
      margin-bottom: 10px;
      letter-spacing: 0.04em;
    }
    .welcome-desc {
      color: #909ab9;
      font-size: 0.98rem;
      margin-bottom: 22px;
      font-weight: 500;
      line-height: 1.65;
      max-width: 330px;
    }
    .continue-btn {
      background: #d4d9ff;
      font-weight: 700;
      color: #7157d3;
      border: none;
      border-radius: 12px;
      font-size: 1.08rem;
      padding: 10px 26px;
      box-shadow: 0 2px 10px #b0b2d860;
      transition: background 0.21s;
    }
    .continue-btn:hover {
      background: #7157d3;
      color: #fff;
    }
    .hero-img {
      flex: 1;
      text-align: right;
    }
    .hero-img img {
      max-width: 360px;
      width: 100%;
      border-radius: 1.8rem;
      box-shadow: 0 6px 24px #abaae633;
      background: #ecefff;
      margin-right: 0;
    }
    @media (max-width:900px) {
      .main-content { flex-direction: column; padding:40px 14px; gap:26px;}
      .hero-img { text-align:center; }
    }
    @media (max-width:700px) {
      .main-wrapper { margin:10px; border-radius:14px; }
      .top-navbar { padding:16px 18px 0 18px; }
      .main-content { padding:24px 8px; }
    }
  </style>
</head>
<body>
  <div class="main-wrapper">
    <!-- Navbar -->
    <div class="top-navbar">
      <div class="nav-logo">LOGOME</div>
      <div class="nav-group">
        <a href="/" class="nav-link active">Home</a>
        <a href="/about" class="nav-link">About</a>
        <a href="/help" class="nav-link">Services</a>
        <a href="/help" class="nav-link">Contact</a>
        <button class="nav-signup">Sign Up</button>
      </div>
    </div>
    <!-- Main Hero -->  
    <div class="main-content">
      <div class="welcome-block">
        <div class="welcome-title">WELCOME</div>
        <div class="welcome-sub">GuidanceX</div>
        <div class="welcome-desc">"Step into your personalized learning space.
Your growth journey begins now."</div>
        <button class="/topics">Continue</button>
      </div>
      <div class="hero-img">
        <img src="/images/welcome-graphic.png" alt="Welcome illustration" />
        <!-- Replace with your SVG or PNG. -->
      </div>
    </div>
  </div>
</body>
</html>
