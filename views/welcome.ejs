<!DOCTYPE html>
<html lang="en">
<head>
  <title>Welcome | GuidanceX</title>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <style>
    /* ---------------- GLOBAL ---------------- */
    body {
      margin: 0;
      font-family: "Montserrat", sans-serif;
      background: linear-gradient(135deg, #a78bfa, #c7d2fe);
      min-height: 100vh;
    }

    /* ---------------- NAVBAR ---------------- */
    .main-nav {
      background: #ffffff;
      padding: 1rem 3rem;
      display: flex;
      align-items: center;
      justify-content: space-between;
      border-radius: 0 0 20px 20px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
    }

    .nav-links a {
      margin: 0 1.3rem;
      color: #4b4b4b;
      font-weight: 600;
      text-decoration: none;
    }

    .nav-links a:hover {
      color: #6c4bf4;
    }

    .btn-signup {
      background: #c7b2ff;
      border: none;
      padding: 6px 18px;
      border-radius: 12px;
      color: #fff;
      font-weight: 700;
      cursor: pointer;
      box-shadow: 0 3px 12px #c7b2ff90;
    }

    /* ---------------- HERO SECTION ---------------- */
    .hero {
      background: #fff;
      max-width: 1000px;
      margin: 60px auto;
      padding: 40px 50px;
      border-radius: 30px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.12);
    }

    .hero-content {
      flex: 1;
      padding-right: 40px;
    }

    .hero-title {
      font-size: 3rem;
      font-weight: 800;
      color: #4b3dbf;
    }

    .hero-sub {
      margin-top: 10px;
      font-size: 1.3rem;
      color: #e8a10d;
      font-weight: 700;
    }

    .hero-desc {
      margin-top: 18px;
      color: #72738a;
      line-height: 1.6;
      font-size: 1rem;
      width: 90%;
    }

    .btn-continue {
      margin-top: 34px;
      background: #e6e6ff;
      border: none;
      padding: 14px 40px;
      border-radius: 18px;
      color: #4b3dbf;
      font-size: 1.2rem;
      font-weight: 700;
      cursor: pointer;
      box-shadow: 0 5px 16px #d4ccff80;
    }

    .btn-continue:hover {
      background: #4b3dbf;
      color: #fff;
    }

    /* ---------------- HERO IMAGE ---------------- */
    .hero-img img {
      width: 360px;
      max-width: 90%;
      filter: drop-shadow(0 10px 22px #b8a7ff60);
    }

    /* ---------------- RESPONSIVE ---------------- */
    @media (max-width: 900px) {
      .hero {
        flex-direction: column;
        text-align: center;
        padding: 40px 30px;
      }
      .hero-content {
        padding-right: 0;
      }
      .hero-img {
        margin-top: 25px;
      }
    }

    @media (max-width: 500px) {
      .hero-title { font-size: 2.2rem; }
      .btn-continue { width: 100%; }
    }
  </style>
</head>
<body>

  <!-- NAVIGATION -->
  <nav class="main-nav">
    <div class="fw-bold fs-3" style="color:#4b3dbf;">GuidanceX</div>

    <div class="nav-links">
      <a href="/">Home</a>
      <a href="/about">About</a>
      <a href="/help">Services</a>
      <a href="/help">Contact</a>
      <button class="btn-signup">Sign Up</button>
    </div>
  </nav>

  <!-- HERO -->
  <section class="hero">
    <div class="hero-content">
      <div class="hero-title">WELCOME</div>
      <div class="hero-sub">GuidanceX</div>
      <p class="hero-desc">
        "Step into your personalized learning space.
Your growth journey begins now."
      </p>

      <form action="/topics">
        <button type="submit" class="btn-continue">Continue</button>
      </form>
    </div>

    <div class="hero-img">
      <!-- Illustration similar to sample -->
      <img src="https://i.ibb.co/7kp4k6p/landing-illustration.png" alt="Illustration">
    </div>
  </section>

</body>
</html>
