<!DOCTYPE html>
<html lang="en">
<head>
  <title>Welcome | GuidanceX</title>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap');

    /* ---- GLOBAL ---- */
    html, body {
      margin: 0;
      padding: 0;
      height: 100%;
      width: 100%;
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #0f1117, #1d1f27);
      color: #f7f7fa;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    /* ---- MAIN CARD ---- */
    .welcome-container {
      background: rgba(30, 32, 40, 0.85);
      border-radius: 30px;
      width: 460px;
      max-width: 90vw;
      padding: 55px 48px;
      text-align: center;
      box-shadow: 0 10px 70px rgba(0,0,0,0.65);
      backdrop-filter: blur(12px);
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    /* ---- HEADING ---- */
    h1 {
      font-weight: 700;
      font-size: 2.6rem;
      letter-spacing: 0.03em;
      margin-bottom: 24px;
      color: #ffffff;
      text-shadow: 0 0 18px #5ebcffaa;
    }

    /* ---- DESCRIPTION ---- */
    p {
      font-weight: 400;
      font-size: 1.15rem;
      color: #cdd0d6;
      line-height: 1.55;
      margin-bottom: 35px;
      max-width: 330px;
    }

    /* ---- FLASH MESSAGE (SUCCESS/INFO) ---- */
    .flash-message {
      width: 100%;
      background: #2d3546cc;
      padding: 14px;
      margin-bottom: 32px;
      border-radius: 16px;
      font-weight: 600;
      font-size: 1.08rem;
      color: #56c0fe;
      box-shadow: 0 0 25px #56c0fe66;
    }

    /* ---- BUTTON ---- */
    button {
      border: none;
      border-radius: 18px;
      padding: 15px 0;
      font-weight: 700;
      font-size: 1.28rem;
      width: 100%;
      max-width: 280px;
      cursor: pointer;
      background: linear-gradient(90deg, #5e8bff, #74bfff);
      color: white;
      box-shadow: 0 7px 28px #5e8bffaa;
      transition: 0.25s ease;
    }

    button:hover {
      background: linear-gradient(90deg, #739eff, #4b72ee);
      box-shadow: 0 10px 36px #4b72eecc;
      transform: translateY(-2px);
    }

    /* ---- SUBTEXT ---- */
    .footer-text {
      margin-top: 40px;
      font-size: 0.95rem;
      color: #92939c;
    }

    /* ---- RESPONSIVE ---- */
    @media (max-width: 480px) {
      .welcome-container {
        width: 95vw;
        padding: 40px 26px;
        border-radius: 25px;
      }
      h1 {
        font-size: 2.1rem;
      }
      p {
        font-size: 1rem;
      }
      button {
        max-width: 100%;
        font-size: 1.18rem;
      }
    }
  </style>
</head>
<body>

  <div class="welcome-container">

    <% if(msg){ %>
      <div class="flash-message"><%= msg %></div>
    <% } %>

    <h1>Welcome, Charan!</h1>

    <p>
      Unlock expert courses in Business, Trading, Design, Freelancing,
      and Personal Growth.<br>
      Your journey to mastery starts here.
    </p>

    <form action="/topics" method="GET">
      <button type="submit">Get Started</button>
    </form>

  </div>

</body>
</html>
