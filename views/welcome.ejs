
<!DOCTYPE html>
<html lang="en">
<head>
  <title>Welcome | GuidanceX</title>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      margin: 0;
      min-height: 100vh;
      font-family: 'Montserrat', Arial, sans-serif;
      background: radial-gradient(ellipse at top left, #ccbafc 0%, #cce1fc 70%, #b9c7e7 100%);
    }

    /* NAVBAR */
    .main-nav {
      background: #fff;
      box-shadow: 0 2px 16px #867ee033;
      padding: 0.8rem 2rem;
      display: flex;
      align-items: center;
      justify-content: space-between;
      border-radius: 0 0 18px 18px;
    }
    .nav-links a {
      margin: 0 1.2rem;
      color: #444;
      font-weight: 600;
      text-decoration: none;
      font-size: 1.07rem;
      letter-spacing: 0.04em;
    }
    .nav-links a.active,
    .nav-links a:hover {
      color: #6546b7;
      background: #e7e4f5;
      border-radius: 8px;
      padding: 4px 12px;
      transition: background 0.22s;
    }
    .btn-signup {
      background: #b0a7fe;
      color: #fff;
      font-weight: 600;
      padding: 6px 22px;
      border-radius: 14px;
      border: none;
      font-size: 1rem;
      box-shadow: 0 2px 12px #b0a7fe77;
      margin-left: 12px;
      transition: background 0.22s;
    }
    .btn-signup:hover { background: #6546b7; }

    /* HERO SECTION */
    .hero {
      display: flex;
      flex-direction: row;
      align-items: center;
      justify-content: space-between;
      max-width: 960px;
      margin: 70px auto 0 auto;
      padding: 0 2rem;
      min-height: 420px;
      border-radius: 2.5rem;
      background: #fff;
      box-shadow: 0 16px 40px #d6daf955;
    }

    .hero-content {
      flex: 1;
      padding-right: 32px;
    }
    .hero-title {
      font-size: 2.8rem;
      font-weight: 800;
      color: #6546b7;
      margin-bottom: 0.6rem;
    }
    .hero-subtitle {
      font-size: 1.25rem;
      font-weight: 700;
      color: #d9a521;
      margin-bottom: 0.7rem;
    }
    .hero-desc {
      color: #7b7b9d;
      font-size: 1rem;
      margin-bottom: 2rem;
      line-height: 1.6;
    }

    .continue-btn{
      background: #e1e3fe;
      color: #6546b7;
      font-weight: 700;
      border-radius: 16px;
      border: none;
      font-size: 1.15rem;
      padding: 13px 35px;
      box-shadow: 0 2px 16px #c5bafc66;
      transition: background 0.22s;
    }
    .continue-btn:hover{
      background: #6546b7;
      color: #fff;
    }

   


    /* HERO IMAGE */
    .hero-img {
      flex: 1;
      text-align: center;
    }
    .hero-img img {
      max-width: 95%;
      width: 340px;
      border-radius: 1.5rem;
      filter: drop-shadow(0 10px 24px #b0a7fe55);
      margin-top: 10px;
    }

    /* MOBILE */
    @media (max-width:900px) {
      .hero {
        flex-direction: column;
        text-align: center;
        padding: 2rem 1rem;
      }
      .hero-content { padding: 0; }
      .hero-img { margin-top: 20px; }
      .hero-img img { width: 280px; }
    }






    .gx-mini-footer{
  width:100%;
  background:#060608;
  border-top:1px solid #232326;
  padding:12px 32px;
  font-family:'Montserrat','Segoe UI',sans-serif;
  font-size:0.8rem;
  color:#9d9daa;
}

.gx-mini-inner{
  max-width:1180px;
  margin:0 auto;
  display:flex;
  justify-content:space-between;
  align-items:center;
  gap:14px;
  flex-wrap:wrap;
}

.gx-mini-main{
  white-space:nowrap;
}

.gx-mini-groups{
  display:flex;
  flex-wrap:wrap;
  gap:14px 26px;
}

.gx-mini-group{
  display:flex;
  flex-wrap:wrap;
  align-items:center;
  gap:6px;
}

.gx-mini-group strong{
  color:#d4d4dd;
  font-weight:600;
}

.gx-mini-group a{
  color:#b7b7c3;
  text-decoration:none;
  position:relative;
  padding-left:8px;
}

.gx-mini-group a::before{
  content:"•";
  position:absolute;
  left:0;
  color:#555561;
  font-size:0.7rem;
  top:0;
}

.gx-mini-group a:hover{
  color:#ff7700;
}

@media (max-width:700px){
  .gx-mini-inner{
    align-items:flex-start;
  }
}

  </style>
</head>

<body>

  <!-- NAV BAR -->
  <nav class="main-nav">
    <div class="nav-logo fw-bold fs-4" style="color:#6546b7;">GuidanceX</div>
    <div class="nav-links">
      <a href="/">Home</a>
      <a href="/about">About</a>
      <a href="/help">Services</a>
      <a href="/help">Contact</a>
      <button class="btn-signup">Sign Up</button>
    </div>
  </nav>

  <!-- HERO -->
  <section class="hero">
    <div class="hero-content">
      <div class="hero-title">WELCOME</div>
      <div class="hero-subtitle">GuidanceX</div>

      <div class="hero-desc">
        "Step into your personalized learning space.<br>
        Your growth journey begins now."
      </div> 
       
      <form action="/topics" >
        <button class="continue-btn">Continue</button>
      </form>
    </div>

   <div class="hero-img">
  <img src="/welcome.jpeg" alt="Welcome Image" />
</div>

  </section>








</body>
</html>
