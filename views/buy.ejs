<!DOCTYPE html>
<html lang="en">
<head>
  <title>Buy <%= course.name %> | GuidanceX</title>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      background: linear-gradient(135deg, #121215, #1c1d2a);
      color: white;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      padding: 72px 24px 36px 24px;
      min-height: 100vh;
    }
    .container {
      max-width: 600px;
      margin: auto;
      background-color: rgba(24, 24, 29, 0.85);
      border-radius: 20px;
      padding: 30px;
    }
    h1 {
      color: #ff7700;
      text-align: center;
      margin-bottom: 28px;
    }
    .expert-info {
      display: flex;
      gap: 18px;
      align-items: center;
      margin-bottom: 28px;
    }
    .expert-photo {
      border-radius: 50%;
      width: 96px;
      height: 96px;
      object-fit: cover;
      border: 3px solid #ff7700;
      box-shadow: 0 0 25px #ff770055;
    }
    .details {
      font-size: 1.11rem;
      line-height: 1.5;
    }
    .details p {
      margin: 8px 0;
    }
    .btn-pay {
      display: block;
      margin: 30px auto 0 auto;
      background: #ff7700;
      border: none;
      border-radius: 18px;
      padding: 16px 48px;
      font-size: 1.3rem;
      font-weight: 700;
      color: #fff;
      cursor: pointer;
      box-shadow: 0 6px 36px #ff770033;
      transition: background 0.3s ease;
    }
    .btn-pay:hover {
      background: #ffa12a;
      box-shadow: 0 8px 44px #ffa12acc;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Buy <%= course.name %></h1>
    <div class="expert-info">
      <img src="<%= course.expertPhoto %>" alt="Expert <%= course.expertName %>" class="expert-photo" />
      <div class="details">
        <p><strong>Expert:</strong> <%= course.expertName %></p>
        <p><strong>Duration:</strong> <%= course.duration %> days</p>
        <p><strong>Price:</strong> ₹<%= course.price %></p>
      </div>
    </div>
    <p class="details"><%= course.description %></p>
    <button class="btn-pay" onclick="location.href='/payment/<%= course.topicName %>/<%= course.name %>'">Proceed to Payment</button>
  </div>
</body>
</html>
