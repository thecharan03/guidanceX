<!DOCTYPE html>
<html lang="en" >
<head>
  <meta charset="UTF-8" />
  <title>Login | GuidanceX</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    @import url('https://fonts.googleapis.com/css2?family=SF+Pro+Text:wght@400;600&display=swap');

    body, html {
      margin: 0;
      height: 100%;
      background: #1c1c1e;
      font-family: 'SF Pro Text', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen,
        Ubuntu, Cantarell, "Open Sans", "Helvetica Neue", sans-serif;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #f5f5f7;
    }

    .login-container {
      background: #2b2b2e;
      border-radius: 30px;
      box-shadow: 0 12px 36px rgb(0 0 0 / 0.5);
      width: 380px;
      max-width: 90vw;
      padding: 45px 35px 45px 35px;
      box-sizing: border-box;
      text-align: center;
    }

    h2 {
      font-weight: 700;
      font-size: 2.4rem;
      margin-bottom: 30px;
      letter-spacing: 0.025em;
      color: #f5f5f7;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 22px;
    }

    input {
      height: 44px;
      font-size: 1.15rem;
      font-weight: 500;
      padding: 0 18px;
      border-radius: 12px;
      border: 1.5px solid #444;
      background: #3a3a3d;
      color: #e0e0e0;
      box-sizing: border-box;
      transition: border-color 0.3s ease;
      outline-offset: 2px;
    }

    input::placeholder {
      color: #a2a2a8;
    }

    input:focus {
      border-color: #609dfa;
      box-shadow: 0 0 12px #609dfa88;
      background: #3f3f43;
      outline: none;
    }

    button {
      margin-top: 10px;
      border: none;
      border-radius: 13px;
      padding: 13px 0;
      font-weight: 700;
      font-size: 1.3rem;
      color: white;
      background: linear-gradient(90deg, #0a84ff, #5ac8fa);
      cursor: pointer;
      transition: background 0.3s ease;
      box-shadow: 0 6px 16px -2px rgb(10 133 255 / 0.6);
    }

    button:hover {
      background: linear-gradient(90deg, #0d71e5, #2da1d5);
      box-shadow: 0 8px 22px -2px rgb(13 113 229 / 0.8);
    }

    .error-message {
      background: #462626;
      color: #ff6161;
      padding: 9px 12px;
      border-radius: 10px;
      font-weight: 600;
      margin-bottom: 14px;
    }

    .footer-text {
      margin-top: 35px;
      font-size: 0.92rem;
      color: #6a6a6d;
      font-weight: 400;
    }

    @media (max-width: 440px) {
      .login-container {
        width: 95vw;
        padding: 40px 30px 40px 30px;
      }
      h2 {
        font-size: 2rem;
      }
      button {
        font-size: 1.15rem;
      }
    }
  </style>
</head>
<body>
  <div class="login-container">
    <h2>Welcome Back</h2>
    <% if(error) { %>
      <div class="error-message"><%= error %></div>
    <% } %>
    <form action="/login" method="POST" novalidate>
      <input type="text" name="username" placeholder="Username" required autofocus autocomplete="username" />
      <input type="password" name="password" placeholder="Password" required autocomplete="current-password" />
      <button type="submit">Sign In</button>
    </form>
    <div class="footer-text">
      Forgot your password? Contact support.
    </div>
  </div>
</body>
</html>
