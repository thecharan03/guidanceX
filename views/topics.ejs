<!DOCTYPE html>
<html lang="en">
<head>
  <title>Code Realm | Akhil Trivedi</title>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.9.1/font/bootstrap-icons.css" rel="stylesheet" />
  <link rel="stylesheet" href="/style.css" />
  <style>
    html, body {
      margin: 0; padding: 0; min-height: 100vh; width: 100vw;
      font-family: 'Montserrat', Arial, sans-serif;
      background: linear-gradient(120deg,#191d24,#1e2236);
      color: #f1f1f1;
      display: flex; flex-direction: column; align-items: center;
    }
    /* Navigation and header styles - same as GuidanceX */
    nav {
      position: fixed; top: 0; left: 0; right: 0; z-index: 1000;
      background: rgba(22,25,33,0.9);
      backdrop-filter: saturate(180%) blur(15px);
      box-shadow: 0 0 2px #111;
      display: flex; justify-content: space-between; align-items: center;
      padding: 0 42px; height: 74px; border-bottom: 1.4px solid #3f4f7825;
    }
    .nav-logo { display: flex; align-items: center; gap: 14px; font-weight: 600; font-size: 1.35rem; color: #efefee; }
    .nav-logo svg { width: 44px; height: 44px; border-radius: 14px; box-shadow: 0 0 2px #111; }
    .nav-links { display: flex; gap: 28px; }
    .nav-links a { color: #aab4cc; font-size: 1rem; font-weight: 500; text-decoration: none; padding: 6px 16px; border-radius: 16px; transition: color 0.18s, background 0.18s;}
    .nav-links a:hover { background: #074e81; color: #00b1ff; text-shadow: 0 0 12px #00bfff;}
    .nav-actions button { background: linear-gradient(20deg, #00b1ff, #006cbf); border: none; border-radius: 16px; font-weight: 600; font-size: 1.02rem; padding: 10px 28px; color: #fff; box-shadow: 0 0 2px #111; cursor: pointer; transition: background 0.22s ease; margin-left: 16px; }
    .nav-actions button:hover { background: linear-gradient(90deg, #0079ca, #004d82); box-shadow: 0 5px 14px #0079ca66; }
    main { margin-top: 94px; padding: 0 2rem 44px 2rem; max-width: 1240px; width: 100%; box-sizing: border-box; }
    h1 { font-weight: 800; font-size: 3.2rem; text-align: center; margin-bottom: 48px; color: #00aaffee; text-shadow: 0 0 15px #00456eaa; user-select: none; }
    
    /* Courses/Projects Grid Styles */
    .projects-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); gap: 38px 36px; }
    .project-card {
      background: #232f41bb;
      border-radius: 28px;
      box-shadow: 0 0 3px #206ea766, 0 0 6px #00baff33;
      padding: 0;
      display: flex; flex-direction: column; align-items: center; cursor: pointer; transition: all 0.22s ease; user-select: none; overflow: hidden;
    }
    .project-card:hover {
      box-shadow: 0 0 9px #00aaff77, 0 4px 12px #008fcc88;
      background: #112230cc;
      transform: scale(1.04);
    }
    .project-thumb {
      width: 100%; height: 180px; object-fit: cover;
      border-radius: 28px 28px 0 0;
      box-shadow: 0 0 7px #2066fe77;
    }
    .project-title {
      font-size: 1.18rem; font-weight: 700; color: #00baff; margin: 22px 0 8px 0; letter-spacing: 0.03em; text-align: center;
    }
    .project-desc {
      font-size: 1.02rem; color: #ccd8ff; text-align: center; margin-bottom: 20px;
      padding: 0 16px;
    }
    .project-action {
      background: linear-gradient(140deg, #2e6bff, #57a8ff 66%);
      border: none; border-radius: 18px; font-weight: 700; font-size: 1.12rem; padding: 14px 28px;
      box-shadow: 0 4px 12px #2066fe88; color: #e0f3ff; width: 90%; max-width: 210px; cursor: pointer; margin-bottom: 18px;
      transition: background 0.22s ease;
    }
    .project-action:hover { background: linear-gradient(90deg, #005e9e 0%, #00b1ff 100%); box-shadow: 0 5px 22px #1b66fe88; }
    @media (max-width: 720px) {
      .projects-grid { grid-template-columns: repeat(auto-fit,minmax(180px,1fr)); }
      .project-title { font-size: 1.07rem; }
      .project-action { font-size: 1rem; padding: 12px 16px; max-width: 170px; }
    }
  </style>
</head>
<body>
  <!-- Navigation -->
  <nav>
    <div class="nav-logo">
      <svg viewBox="0 0 55 39"><rect x="12" y="8" width="28" height="8" rx="3.2" fill="#ff7700" stroke="#fff" stroke-width="2"/><rect x="16.5" y="16" width="19" height="9" rx="3.2" fill="#232326" stroke="#ff7700" stroke-width="1.5"/><polygon points="20,25 30,32 26,34 36,37 28,27" fill="#fff" stroke="#ff7700" stroke-width="1"/><circle cx="28" cy="12" r="3.2" fill="#fff" stroke="#ff7700" stroke-width="2"/></svg>
      Akhil Trivedi's Code Realm
    </div>
    <div class="nav-links">
      <a href="/">Home</a>
      <a href="#projects">Projects</a>
      <a href="#about">About</a>
      <a href="#contact">Contact</a>
    </div>
    <div class="nav-actions">
      <a href="/login"><button>Log In</button></a>
      <a href="/#projects"><button>View Work</button></a>
    </div>
  </nav>
  <!-- Header -->
  <main>
    <h1 id="projects">Latest Projects</h1>
    <section class="projects-grid">
      <!-- Example cards, swap img src for your real project images -->
      <div class="project-card" onclick="window.open('https://yourprojectlink.com')">
        <img class="project-thumb" src="/uploads/image.jpg" alt="Design Project 1" />
        <h3 class="project-title">Get Designs</h3>
        <p class="project-desc">Showcase of creative UI and design solutions.</p>
        <button class="project-action">View Details</button>
      </div>
      <div class="project-card">
        <img class="project-thumb" src="/uploads/your-tech-image.jpg" alt="Computing Wonders" />
        <h3 class="project-title">Computing Wonders</h3>
        <p class="project-desc">Enchanting code; modern CS meets aesthetics.</p>
        <button class="project-action">Learn More</button>
      </div>
      <div class="project-card">
        <img class="project-thumb" src="/uploads/your-learning-image.jpg" alt="Learning Tech" />
        <h3 class="project-title">Learning Tech</h3>
        <p class="project-desc">Hands-on coding guides and tutorials.</p>
        <button class="project-action">Start Learning</button>
      </div>
      <!-- Add more cards as desired -->
    </section>
    <!-- Add any 'Akhil's Exploits' or other sections as desired -->
  </main>
</body>
</html>
